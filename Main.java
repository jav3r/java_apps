package code;

import java.awt.*;

public class Main {
    public static void main(String[] args) {
        Reader r = new Reader();
        r.setLayout(new FlowLayout());
        r.setVisible(true);
        r.setSize(240, 210);
        r.setLocationRelativeTo(null);
    }
}